{
  "name": "my-scrollytelling-site",
  "private": true,
  "scripts": {
    "build": "cd website && npm run build",
    "dev": "cd website && npm run dev",
    "start": "cd website && npm run start"
  }
}
